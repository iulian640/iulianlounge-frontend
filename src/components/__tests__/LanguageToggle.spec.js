import { describe, it } from 'vitest';

// Pendiente (IUL-26): reescribir con TDD el conmutador ES/EN — sesión con Opus.
// La v1 del test está en el historial del 2026-07-12; ojo al typo
// '@vue/tets-utils' en el import (ese era el segundo rojo).
describe('LanguageToggle', () => {
  it.todo('cambia el idioma a EN al pulsar EN');
});
